
CubeSatSite
===========

The CubeSat Laboratory web site.

The `doc` and `PUBLIC` folders are an integral part of the website, but they are not under
version control (at the moment). They contain some large files, so Git-LFS should be configured.
Right now their content is on my (pchapin's) master version of the site.
